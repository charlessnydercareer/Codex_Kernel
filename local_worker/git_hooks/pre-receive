#!/usr/bin/env bash
set -euo pipefail

HOOK_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
GIT_DIR="$(cd "${HOOK_DIR}/.." && pwd)"
WORKER_DIR="$(cd "${GIT_DIR}/../local_worker" && pwd)"

exec python3 "${WORKER_DIR}/src/pre_receive_gate.py" --git-dir "${GIT_DIR}"
